{
  "name": "notapipe",
  "version": "0.1.0",
  "private": false,
  "url": "https://notapipe.app",
  "type": "module",
  "description": "Notapipe is a peer-to-peer collaboration tool for sharing text, voice, and files. It uses WebRTC for direct connections between users, eliminating the need for servers to handle your data. Notapipe is ideal for quick sharing sessions, brainstorming, or any situation where you want to collaborate without leaving a trace.",
  "scripts": {
    "dev": "pnpm -r --parallel run dev",
    "build": "cd packages/client && pnpm run build",
    "start": "node --experimental-strip-types ./packages/signalling/src/index.ts",
    "test": "pnpm test",
    "check": "pnpm check",
    "kill:port": "sh -c 'lsof -ti :$0 | xargs kill -9'"
  },
  "dependencies": {
    "marked": "^18.0.0"
  },
  "devDependencies": {
    "jsdom": "^29.0.1",
    "vitest": "^4.1.2"
  },
  "packageManager": "pnpm@10.33.0",
  "pnpm": {
    "onlyBuiltDependencies": [
      "esbuild"
    ]
  }
}
