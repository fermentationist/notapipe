export const WORDLIST_SIZE = 2048;
export const ROOM_WORD_COUNT = 3;
export const PASSPHRASE_WORD_COUNT = 2;
export const RATE_LIMIT_JOIN_PER_MINUTE = 10;
// Grid cell size in degrees for geo mode. 0.001° ≈ 111 m latitude; longitude
// varies by ~cos(lat) but is still a good-enough "nearby" threshold.
export const GEO_GRID_PRECISION = 0.001;
