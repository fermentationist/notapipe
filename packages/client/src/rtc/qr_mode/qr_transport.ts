// QrTransport — SignalTransport implementation that wraps the QR encode/decode flow
// QR mode waits for iceGatheringState === "complete" (15s timeout) before encoding

import type { SignalTransport } from "../peer.ts";

export {};
