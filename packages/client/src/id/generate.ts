// Room ID generation and parsing
// generateId() — picks 3 random words via crypto.getRandomValues()
// parseId()   — reads from window.location.pathname
// isValidId() — validates a 3-word phrase against the wordlist

export {};
